import { MeatsPipePipe } from './meats-pipe.pipe';

describe('MeatsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MeatsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
